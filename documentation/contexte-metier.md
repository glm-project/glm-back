# Contexte métier

Décrit les bounded contexts métier du projet et leur rôle. Les règles de code (architecture, DDD, tests, conventions) sont dans `glm-back/CLAUDE.md`.

## elementdefabrication

Gère les éléments de fabrication : ordres de fabrication (`OrdreDeFabrication`) et produits (`Produit`), avec numérotation automatique du nom quand il n'est pas fourni à la création.
